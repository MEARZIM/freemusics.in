export interface Album {
    id: string;
    mainImageUrl: string;
    coverImageUrl: string;
    name: string;
    cover: string;
    artistId: string;
    createdAt: string;
    updatedAt: string;
}